name: Build and Push
run-name: ${{ github.actor }} is building and pushing
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: uname -a
      - run: ls -lrth

